/*
    Compile unit: C:\branches\RT_First_Playable\RT\Engine\Core\x\xGrid.h
    Producer: MW EABI PPC C-Compiler
    Language: C++
    Code range: 0x80017A90 -> 0x80017BF4
*/
// Range: 0x80017A90 -> 0x80017B84
void xGridCheckBound(struct xGrid & grid /* r26 */, const struct xBound & bound /* r4 */, const struct xQCData & qcd /* r27 */, struct cache_entity_callback & cb /* r28 */) {
    // Local variables
    struct xGridIterator it; // r1+0x18
    struct xBox box; // r1+0x24
    int lower_x; // r1+0x14
    int lower_z; // r1+0x10
    int upper_x; // r1+0xC
    int upper_z; // r1+0x8
    struct xGridBound * cell; // r31
    int gx; // r30
    int gz; // r29
}

// Range: 0x80017B84 -> 0x80017BF4
struct xGridBound * xGridIterNextCell(struct xGridIterator & it /* r3 */) {}


/*
    Compile unit: C:\branches\RT_First_Playable\RT\Engine\Core\x\xGrid.h
    Producer: MW EABI PPC C-Compiler
    Language: C++
    Code range: 0x8002454C -> 0x80024640
*/
// Range: 0x8002454C -> 0x80024640
void xGridCheckBound(struct xGrid & grid /* r26 */, const struct xBound & bound /* r4 */, const struct xQCData & qcd /* r27 */, struct OBBHitsEntCB & cb /* r28 */) {
    // Local variables
    struct xGridIterator it; // r1+0x18
    struct xBox box; // r1+0x24
    int lower_x; // r1+0x14
    int lower_z; // r1+0x10
    int upper_x; // r1+0xC
    int upper_z; // r1+0x8
    struct xGridBound * cell; // r31
    int gx; // r30
    int gz; // r29
}


/*
    Compile unit: C:\branches\RT_First_Playable\RT\Engine\Core\x\xGrid.h
    Producer: MW EABI PPC C-Compiler
    Language: C++
    Code range: 0x80045C20 -> 0x80045E08
*/
// Range: 0x80045C20 -> 0x80045D14
void xGridCheckBound(struct xGrid & grid /* r26 */, const struct xBound & bound /* r4 */, const struct xQCData & qcd /* r27 */, struct cb_decal_sphere_hits_ent & cb /* r28 */) {
    // Local variables
    struct xGridIterator it; // r1+0x18
    struct xBox box; // r1+0x24
    int lower_x; // r1+0x14
    int lower_z; // r1+0x10
    int upper_x; // r1+0xC
    int upper_z; // r1+0x8
    struct xGridBound * cell; // r31
    int gx; // r30
    int gz; // r29
}

// Range: 0x80045D14 -> 0x80045E08
void xGridCheckBound(struct xGrid & grid /* r26 */, const struct xBound & bound /* r4 */, const struct xQCData & qcd /* r27 */, struct cb_decal_hits_ent & cb /* r28 */) {
    // Local variables
    struct xGridIterator it; // r1+0x18
    struct xBox box; // r1+0x24
    int lower_x; // r1+0x14
    int lower_z; // r1+0x10
    int upper_x; // r1+0xC
    int upper_z; // r1+0x8
    struct xGridBound * cell; // r31
    int gx; // r30
    int gz; // r29
}


/*
    Compile unit: C:\branches\RT_First_Playable\RT\Engine\Core\x\xGrid.h
    Producer: MW EABI PPC C-Compiler
    Language: C++
    Code range: 0x80050B78 -> 0x80050B78
*/

/*
    Compile unit: C:\branches\RT_First_Playable\RT\Engine\Core\x\xGrid.h
    Producer: MW EABI PPC C-Compiler
    Language: C++
    Code range: 0x8006DA58 -> 0x8006DA58
*/

/*
    Compile unit: C:\branches\RT_First_Playable\RT\Engine\Core\x\xGrid.h
    Producer: MW EABI PPC C-Compiler
    Language: C++
    Code range: 0x800796EC -> 0x800797E0
*/
// Range: 0x800796EC -> 0x800797E0
void xGridCheckBound(struct xGrid & grid /* r26 */, const struct xBound & bound /* r4 */, const struct xQCData & qcd /* r27 */, class xLightCollisionCallback & cb /* r28 */) {
    // Local variables
    struct xGridIterator it; // r1+0x18
    struct xBox box; // r1+0x24
    int lower_x; // r1+0x14
    int lower_z; // r1+0x10
    int upper_x; // r1+0xC
    int upper_z; // r1+0x8
    struct xGridBound * cell; // r31
    int gx; // r30
    int gz; // r29
}


/*
    Compile unit: C:\branches\RT_First_Playable\RT\Engine\Core\x\xGrid.h
    Producer: MW EABI PPC C-Compiler
    Language: C++
    Code range: 0x800AE9AC -> 0x800AEAA0
*/
// Range: 0x800AE9AC -> 0x800AEAA0
void xGridCheckBound(struct xGrid & grid /* r26 */, const struct xBound & bound /* r4 */, const struct xQCData & qcd /* r27 */, struct cbRayHitsEnt & cb /* r28 */) {
    // Local variables
    struct xGridIterator it; // r1+0x18
    struct xBox box; // r1+0x24
    int lower_x; // r1+0x14
    int lower_z; // r1+0x10
    int upper_x; // r1+0xC
    int upper_z; // r1+0x8
    struct xGridBound * cell; // r31
    int gx; // r30
    int gz; // r29
}


/*
    Compile unit: C:\branches\RT_First_Playable\RT\Engine\Core\x\xGrid.h
    Producer: MW EABI PPC C-Compiler
    Language: C++
    Code range: 0x80125438 -> 0x80125620
*/
// Range: 0x80125438 -> 0x8012552C
void xGridCheckBound(struct xGrid & grid /* r26 */, const struct xBound & bound /* r4 */, const struct xQCData & qcd /* r27 */, struct cb_sss & cb /* r28 */) {
    // Local variables
    struct xGridIterator it; // r1+0x18
    struct xBox box; // r1+0x24
    int lower_x; // r1+0x14
    int lower_z; // r1+0x10
    int upper_x; // r1+0xC
    int upper_z; // r1+0x8
    struct xGridBound * cell; // r31
    int gx; // r30
    int gz; // r29
}

// Range: 0x8012552C -> 0x80125620
void xGridCheckBound(struct xGrid & grid /* r26 */, const struct xBound & bound /* r4 */, const struct xQCData & qcd /* r27 */, struct cb_ss & cb /* r28 */) {
    // Local variables
    struct xGridIterator it; // r1+0x18
    struct xBox box; // r1+0x24
    int lower_x; // r1+0x14
    int lower_z; // r1+0x10
    int upper_x; // r1+0xC
    int upper_z; // r1+0x8
    struct xGridBound * cell; // r31
    int gx; // r30
    int gz; // r29
}


/*
    Compile unit: C:\branches\RT_First_Playable\RT\Engine\Core\x\xGrid.h
    Producer: MW EABI PPC C-Compiler
    Language: C++
    Code range: 0x80136430 -> 0x80136524
*/
// Range: 0x80136430 -> 0x80136524
void xGridCheckBound(struct xGrid & grid /* r26 */, const struct xBound & bound /* r4 */, const struct xQCData & qcd /* r27 */, struct cb_beam_hits_ent & cb /* r28 */) {
    // Local variables
    struct xGridIterator it; // r1+0x18
    struct xBox box; // r1+0x24
    int lower_x; // r1+0x14
    int lower_z; // r1+0x10
    int upper_x; // r1+0xC
    int upper_z; // r1+0x8
    struct xGridBound * cell; // r31
    int gx; // r30
    int gz; // r29
}


/*
    Compile unit: C:\branches\RT_First_Playable\RT\Engine\Core\x\xGrid.h
    Producer: MW EABI PPC C-Compiler
    Language: C++
    Code range: 0x80154810 -> 0x80154904
*/
// Range: 0x80154810 -> 0x80154904
void xGridCheckBound(struct xGrid & grid /* r26 */, const struct xBound & bound /* r4 */, const struct xQCData & qcd /* r27 */, class depenetrate_callback & cb /* r28 */) {
    // Local variables
    struct xGridIterator it; // r1+0x18
    struct xBox box; // r1+0x24
    int lower_x; // r1+0x14
    int lower_z; // r1+0x10
    int upper_x; // r1+0xC
    int upper_z; // r1+0x8
    struct xGridBound * cell; // r31
    int gx; // r30
    int gz; // r29
}


/*
    Compile unit: C:\branches\RT_First_Playable\RT\Engine\Core\x\xGrid.h
    Producer: MW EABI PPC C-Compiler
    Language: C++
    Code range: 0x801AC528 -> 0x801AC61C
*/
// Range: 0x801AC528 -> 0x801AC61C
void xGridCheckBound(struct xGrid & grid /* r26 */, const struct xBound & bound /* r4 */, const struct xQCData & qcd /* r27 */, class damage_callback & cb /* r28 */) {
    // Local variables
    struct xGridIterator it; // r1+0x18
    struct xBox box; // r1+0x24
    int lower_x; // r1+0x14
    int lower_z; // r1+0x10
    int upper_x; // r1+0xC
    int upper_z; // r1+0x8
    struct xGridBound * cell; // r31
    int gx; // r30
    int gz; // r29
}


/*
    Compile unit: C:\branches\RT_First_Playable\RT\Engine\Core\x\xGrid.h
    Producer: MW EABI PPC C-Compiler
    Language: C++
    Code range: 0x801B7C78 -> 0x801B7D6C
*/
// Range: 0x801B7C78 -> 0x801B7D6C
void xGridCheckBound(struct xGrid & grid /* r26 */, const struct xBound & bound /* r4 */, const struct xQCData & qcd /* r27 */, struct check_bound & cb /* r28 */) {
    // Local variables
    struct xGridIterator it; // r1+0x18
    struct xBox box; // r1+0x24
    int lower_x; // r1+0x14
    int lower_z; // r1+0x10
    int upper_x; // r1+0xC
    int upper_z; // r1+0x8
    struct xGridBound * cell; // r31
    int gx; // r30
    int gz; // r29
}


/*
    Compile unit: C:\branches\RT_First_Playable\RT\Engine\Core\x\xGrid.h
    Producer: MW EABI PPC C-Compiler
    Language: C++
    Code range: 0x801C3244 -> 0x801C3244
*/

/*
    Compile unit: C:\branches\RT_First_Playable\RT\Engine\Core\x\xGrid.h
    Producer: MW EABI PPC C-Compiler
    Language: C++
    Code range: 0x801DCAE4 -> 0x801DCBD8
*/
// Range: 0x801DCAE4 -> 0x801DCBD8
void xGridCheckBound(struct xGrid & grid /* r26 */, const struct xBound & bound /* r4 */, const struct xQCData & qcd /* r27 */, class zRubbleHitCallback & cb /* r28 */) {
    // Local variables
    struct xGridIterator it; // r1+0x18
    struct xBox box; // r1+0x24
    int lower_x; // r1+0x14
    int lower_z; // r1+0x10
    int upper_x; // r1+0xC
    int upper_z; // r1+0x8
    struct xGridBound * cell; // r31
    int gx; // r30
    int gz; // r29
}


/*
    Compile unit: C:\branches\RT_First_Playable\RT\Engine\Core\x\xGrid.h
    Producer: MW EABI PPC C-Compiler
    Language: C++
    Code range: 0x80211068 -> 0x8021115C
*/
// Range: 0x80211068 -> 0x8021115C
void xGridCheckBound(struct xGrid & grid /* r26 */, const struct xBound & bound /* r4 */, const struct xQCData & qcd /* r27 */, struct cbVentHitsBoulder & cb /* r28 */) {
    // Local variables
    struct xGridIterator it; // r1+0x18
    struct xBox box; // r1+0x24
    int lower_x; // r1+0x14
    int lower_z; // r1+0x10
    int upper_x; // r1+0xC
    int upper_z; // r1+0x8
    struct xGridBound * cell; // r31
    int gx; // r30
    int gz; // r29
}


/*
    Compile unit: C:\branches\RT_First_Playable\RT\Engine\Core\x\xGrid.h
    Producer: MW EABI PPC C-Compiler
    Language: C++
    Code range: 0x80231AAC -> 0x80231AAC
*/

/*
    Compile unit: C:\branches\RT_First_Playable\RT\Engine\Core\x\xGrid.h
    Producer: MW EABI PPC C-Compiler
    Language: C++
    Code range: 0x80237A20 -> 0x80237B14
*/
// Range: 0x80237A20 -> 0x80237B14
void xGridCheckBound(struct xGrid & grid /* r26 */, const struct xBound & bound /* r4 */, const struct xQCData & qcd /* r27 */, class grab_collide_callback & cb /* r28 */) {
    // Local variables
    struct xGridIterator it; // r1+0x18
    struct xBox box; // r1+0x24
    int lower_x; // r1+0x14
    int lower_z; // r1+0x10
    int upper_x; // r1+0xC
    int upper_z; // r1+0x8
    struct xGridBound * cell; // r31
    int gx; // r30
    int gz; // r29
}


/*
    Compile unit: C:\branches\RT_First_Playable\RT\Engine\Core\x\xGrid.h
    Producer: MW EABI PPC C-Compiler
    Language: C++
    Code range: 0x8024E848 -> 0x8024E93C
*/
// Range: 0x8024E848 -> 0x8024E93C
void xGridCheckBound(struct xGrid & grid /* r26 */, const struct xBound & bound /* r4 */, const struct xQCData & qcd /* r27 */, class StaticObjectsCallBack & cb /* r28 */) {
    // Local variables
    struct xGridIterator it; // r1+0x18
    struct xBox box; // r1+0x24
    int lower_x; // r1+0x14
    int lower_z; // r1+0x10
    int upper_x; // r1+0xC
    int upper_z; // r1+0x8
    struct xGridBound * cell; // r31
    int gx; // r30
    int gz; // r29
}


/*
    Compile unit: C:\branches\RT_First_Playable\RT\Engine\Core\x\xGrid.h
    Producer: MW EABI PPC C-Compiler
    Language: C++
    Code range: 0x8025C090 -> 0x8025C184
*/
// Range: 0x8025C090 -> 0x8025C184
void xGridCheckBound(struct xGrid & grid /* r26 */, const struct xBound & bound /* r4 */, const struct xQCData & qcd /* r27 */, class CheckForCollisionWrapper & cb /* r28 */) {
    // Local variables
    struct xGridIterator it; // r1+0x18
    struct xBox box; // r1+0x24
    int lower_x; // r1+0x14
    int lower_z; // r1+0x10
    int upper_x; // r1+0xC
    int upper_z; // r1+0x8
    struct xGridBound * cell; // r31
    int gx; // r30
    int gz; // r29
}


/*
    Compile unit: C:\branches\RT_First_Playable\RT\Engine\Core\x\xGrid.h
    Producer: MW EABI PPC C-Compiler
    Language: C++
    Code range: 0x8027DEF0 -> 0x8027DEF0
*/

/*
    Compile unit: C:\branches\RT_First_Playable\RT\Engine\Core\x\xGrid.h
    Producer: MW EABI PPC C-Compiler
    Language: C++
    Code range: 0x8028105C -> 0x80281150
*/
// Range: 0x8028105C -> 0x80281150
void xGridCheckBound(struct xGrid & grid /* r26 */, const struct xBound & bound /* r4 */, const struct xQCData & qcd /* r27 */, class zHumanVehiclePlayerDrive & cb /* r28 */) {
    // Local variables
    struct xGridIterator it; // r1+0x18
    struct xBox box; // r1+0x24
    int lower_x; // r1+0x14
    int lower_z; // r1+0x10
    int upper_x; // r1+0xC
    int upper_z; // r1+0x8
    struct xGridBound * cell; // r31
    int gx; // r30
    int gz; // r29
}


/*
    Compile unit: C:\branches\RT_First_Playable\RT\Engine\Core\x\xGrid.h
    Producer: MW EABI PPC C-Compiler
    Language: C++
    Code range: 0x802870A0 -> 0x80287194
*/
// Range: 0x802870A0 -> 0x80287194
void xGridCheckBound(struct xGrid & grid /* r26 */, const struct xBound & bound /* r4 */, const struct xQCData & qcd /* r27 */, class zSphereAvoidenceSearchCB & cb /* r28 */) {
    // Local variables
    struct xGridIterator it; // r1+0x18
    struct xBox box; // r1+0x24
    int lower_x; // r1+0x14
    int lower_z; // r1+0x10
    int upper_x; // r1+0xC
    int upper_z; // r1+0x8
    struct xGridBound * cell; // r31
    int gx; // r30
    int gz; // r29
}


/*
    Compile unit: C:\branches\RT_First_Playable\RT\Engine\Core\x\xGrid.h
    Producer: MW EABI PPC C-Compiler
    Language: C++
    Code range: 0x802916A4 -> 0x802916A4
*/

/*
    Compile unit: C:\branches\RT_First_Playable\RT\Engine\Core\x\xGrid.h
    Producer: MW EABI PPC C-Compiler
    Language: C++
    Code range: 0x80293D54 -> 0x80293E48
*/
// Range: 0x80293D54 -> 0x80293E48
void xGridCheckBound(struct xGrid & grid /* r26 */, const struct xBound & bound /* r4 */, const struct xQCData & qcd /* r27 */, class zRatBallPlayerDrive & cb /* r28 */) {
    // Local variables
    struct xGridIterator it; // r1+0x18
    struct xBox box; // r1+0x24
    int lower_x; // r1+0x14
    int lower_z; // r1+0x10
    int upper_x; // r1+0xC
    int upper_z; // r1+0x8
    struct xGridBound * cell; // r31
    int gx; // r30
    int gz; // r29
}


/*
    Compile unit: C:\branches\RT_First_Playable\RT\Engine\Core\x\xGrid.h
    Producer: MW EABI PPC C-Compiler
    Language: C++
    Code range: 0x8029BED0 -> 0x8029BFC4
*/
// Range: 0x8029BED0 -> 0x8029BFC4
void xGridCheckBound(struct xGrid & grid /* r26 */, const struct xBound & bound /* r4 */, const struct xQCData & qcd /* r27 */, class zCompLogicWaiter & cb /* r28 */) {
    // Local variables
    struct xGridIterator it; // r1+0x18
    struct xBox box; // r1+0x24
    int lower_x; // r1+0x14
    int lower_z; // r1+0x10
    int upper_x; // r1+0xC
    int upper_z; // r1+0x8
    struct xGridBound * cell; // r31
    int gx; // r30
    int gz; // r29
}


